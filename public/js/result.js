import { api, getSession, writeCart } from '/js/app.js';
const statusEl=document.getElementById('resultStatus'); const detailsEl=document.getElementById('orderDetails');
function formatBRL(v){return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v)}
(async function(){const params=new URLSearchParams(location.search); const orderId=params.get('order_id'); const collectionStatus=params.get('collection_status')||params.get('status')||'pending'; const paymentId=params.get('payment_id')||params.get('collection_id'); if(!orderId){statusEl.className='feedback error'; statusEl.textContent='Pedido não identificado no retorno.'; return;} try{const order=await api(`/api/orders/public/${orderId}`); if(collectionStatus){await api(`/api/orders/${orderId}/return-update`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({paymentStatus:collectionStatus,paymentId})}).catch(()=>{});} statusEl.className=`feedback ${collectionStatus==='approved'?'success':'info'}`; statusEl.textContent=`Checkout retornou com status: ${collectionStatus}`; detailsEl.innerHTML=`<article class="order-card"><header><div><strong>Pedido ${order.id.slice(0,8)}</strong><div class="feedback">${order.customer.name}</div></div></header><div class="order-meta"><div><strong>Total</strong><div>${formatBRL(order.total)}</div></div><div><strong>Itens</strong><div>${order.items.map(item=>`${item.quantity}x ${item.name}`).join(', ')}</div></div><div><strong>Frete</strong><div>${order.shipping?.label||'-'}</div></div><div><strong>Pagamento</strong><div>${paymentId||order.paymentId||'-'}</div></div></div></article>`; if(collectionStatus==='approved'){writeCart([]);} }catch(err){statusEl.className='feedback error'; statusEl.textContent=err.message;}})();

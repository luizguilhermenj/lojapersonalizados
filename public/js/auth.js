import { api, setSession, getSession, getDashboardPath } from '/js/app.js';
if(getSession()) location.href=getDashboardPath();
const loginForm=document.getElementById('loginForm'); const registerForm=document.getElementById('registerForm');
const loginFeedback=document.getElementById('loginFeedback'); const registerFeedback=document.getElementById('registerFeedback');
loginForm?.addEventListener('submit', async e=>{e.preventDefault(); loginFeedback.textContent='Entrando...'; try{const payload=Object.fromEntries(new FormData(loginForm).entries()); const data=await api('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}); setSession(data); loginFeedback.className='feedback success'; loginFeedback.textContent='Login realizado com sucesso.'; setTimeout(()=>location.href=data.user?.role==='admin'?'/pages/admin.html':'/pages/pedidos.html',500);}catch(err){loginFeedback.className='feedback error'; loginFeedback.textContent=err.message;}});
registerForm?.addEventListener('submit', async e=>{e.preventDefault(); registerFeedback.textContent='Criando conta...'; try{const payload=Object.fromEntries(new FormData(registerForm).entries()); const data=await api('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}); setSession(data); registerFeedback.className='feedback success'; registerFeedback.textContent='Conta criada com sucesso.'; setTimeout(()=>location.href='/pages/home.html',500);}catch(err){registerFeedback.className='feedback error'; registerFeedback.textContent=err.message;}});
